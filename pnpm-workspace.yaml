shamefullyHoist: true

overrides:
  sharp: 0.35.3

# These packages' install scripts are not needed for this project's build, so
# they stay unapproved (the same set pnpm 10 skipped by default).
allowBuilds:
  "@parcel/watcher": false
  "@prisma/engines": false
  esbuild: false
  prisma: false
  unrs-resolver: false
  vue-demi: false
