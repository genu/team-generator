export default defineAppConfig({
  ui: {
    input: {
      defaultVariants: {
        size: 'xl',
      },
      slots: {
        root: 'w-full',
      },
    },
    button: {
      defaultVariants: {
        size: 'md',
      },
    },
  },
})
